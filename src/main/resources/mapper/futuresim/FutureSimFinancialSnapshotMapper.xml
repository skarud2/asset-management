<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.via.shinvia.futuresim.mapper.FutureSimFinancialSnapshotMapper">

    <select id="findLiquidAssetAmountByUserId" resultType="java.math.BigDecimal">
        SELECT liquid_asset_amount
        FROM user_financial_profile
        WHERE user_id = #{userId}
    </select>

    <select id="sumAccountBalanceByUserId" resultType="java.math.BigDecimal">
        SELECT SUM(account.current_balance)
        FROM account
        JOIN mydata_connection connection
          ON connection.connection_id = account.connection_id
        WHERE connection.user_id = #{userId}
    </select>

</mapper>

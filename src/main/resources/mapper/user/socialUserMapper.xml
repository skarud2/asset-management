<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.via.shinvia.oauth2.mapper.SocialUserMapper">
    <select id="findByProviderAndProviderUserId">
        SELECT *
        FROM social_user
        WHERE provider = #{provider}
        AND provider_user_id = #{providerUserId}
    </select>

    <insert id="insertSocialUser"
            parameterType="SocialUser"
            useGeneratedKeys="true"
            keyProperty="socialUserId"
            keyColumn="social_user_id">
        INSERT INTO social_user (
            user_id,
            provider,
            provider_user_id,
            provider_email
        )
        VALUES (
            #{userId},
            #{provider},
            #{providerUserId},
            #{providerEmail}
        )
    </insert>

    <select id="findAllByUserId">
        SELECT
            social_user_id,
            user_id,
            provider,
            provider_user_id,
            provider_email,
            created_at
        FROM social_user
        WHERE user_id = #{userId}
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.via.shinvia.policy.recommendation.policyloan.mapper.PolicyLoanRecommendationMapper">

    <!-- 현재 추천 가능한 서민금융·정책대출 상품 조회 -->
    <select id="findActiveProducts"
            resultType="com.via.shinvia.policy.recommendation.policyloan.dto.PolicySupportProductDTO">
        SELECT
        policy_support_program_id AS product_id,
        external_seq,
        program_name AS product_name,
        target_description,
        max_support_amount,
        min_interest_rate,
        max_interest_rate,
        interest_rate_description,
        support_period_description,
        usage_description,
        offering_institution_name,
        handling_institution,
        support_area,
        eligibility_description,
        application_method,
        application_url,
        operation_period,
        CAST(eligibility_json AS CHAR) AS eligibility_json
        FROM policy_support_program
        WHERE active = TRUE
        ORDER BY policy_support_program_id
    </select>

</mapper>

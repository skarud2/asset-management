<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.via.shinvia.mydata.mapper.MyDataConnectionMapper">
    <insert id="insertMyDataConnection"
            parameterType="MyDataConnection"
            useGeneratedKeys="true"
            keyProperty="connectionId"
            keyColumn="connection_id">
        INSERT INTO mydata_connection (
        user_id,
        connection_status
        )
        VALUES (
        #{userId},
        #{connectionStatus}
        )
    </insert>

    <select id="findByUserId">
        SELECT *
        FROM mydata_connection
        WHERE user_id=#{userId}
    </select>

    <select id="findConnectionIdByUserId">
        SELECT connection_id
        FROM mydata_connection
        WHERE user_id=#{userId}
    </select>

    <update id="updateConnected">
        UPDATE mydata_connection
        SET connection_status='CONNECTED',
            connected_at=CURRENT_TIMESTAMP,
            disconnected_at=NULL,
            updated_at=CURRENT_TIMESTAMP
        WHERE connection_id=#{connectionId}
    </update>

    <update id="updateStatus">
        UPDATE  mydata_connection
        SET connection_status=#{connectionStatus},
            updated_at=CURRENT_TIMESTAMP
        WHERE connection_id=#{connectionId}
    </update>
</mapper>
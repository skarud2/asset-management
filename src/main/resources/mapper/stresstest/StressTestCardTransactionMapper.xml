<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.via.shinvia.stresstest.mapper.StressTestCardTransactionMapper">

    <!-- card_account.user_id로 바로 조회 가능 (loan_account와 달리 중간 조인 불필요) -->
    <select id="sumAmountByUserIdSince"
            resultType="java.math.BigDecimal">

        SELECT COALESCE(SUM(ct.amount), 0)
        FROM card_transaction ct
        JOIN card_account ca ON ca.card_account_id = ct.card_account_id
        WHERE ca.user_id = #{userId}
        AND ct.transaction_at &gt;= #{since}

    </select>

</mapper>
